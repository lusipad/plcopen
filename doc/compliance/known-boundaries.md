# 已知边界注册表（Known Boundaries, KB-001+）

> 本文件是新核相对 PLCopen 标准与 v0.x 旧线的**已声明行为边界**的
> normative 注册表（单一事实源，自 README 迁入，2026-07-05）。
> 登记流程见 `.claude/skills/plcopen-kb-boundary`；新号只追加、不重排。
> 测试、issue、PR 说明与代码注释应引用 KB 编号。

分段导读：**KB-001~018** = v0.x 旧线时代的承接口径；**KB-019~026** =
新核相对旧线的基础边界；**KB-027 起** = 新核各批次边界。全表升序，
编号即锚点，按需检索、不必通读。

- `KB-001`：当前 `BufferMode` 已区分 `ABORTING`、`BUFFERED` 与单轴 MoveNode blending：`BLENDING_LOW` / `BLENDING_PREVIOUS` / `BLENDING_NEXT` / `BLENDING_CNC` 在前一条 MoveNode 减速到标称速度 30% 以下时提前接续，`BLENDING_HIGH` 在 70% 以下时提前接续；Homing 和 Sync 节点对非 aborting / blending 模式采用明确的 queued handoff 语义，低速或短距离 MoveNode blending 场景可能退化为 `BUFFERED`。
- `KB-002`：基础 IEC 定时器按显式周期推进；调用方需要保证每 scan 调用一次，并通过 `setCycleTime()` 提供周期时间。
- `KB-003`：`MC_SetOverride` 当前作用于**新规划**的运动命令和 Homing、active 非连续位置运动、active Homing，以及 active `MC_MoveVelocity`、`MC_MoveContinuousAbsolute`、`MC_MoveContinuousRelative`、`MC_PositionProfile`、`MC_VelocityProfile`、`MC_AccelerationProfile` 的重规划；Gear/Cam Sync 节点按主轴值驱动，不作为本地 override planner 节点重规划。
- `KB-004`：`MC_TouchProbe` / `MC_AbortTrigger` 当前基于 Servo 数字输入扩展通道实现上升沿位置捕获、`WindowOnly` 位置窗口门控、多输入软件 armed trigger 存储和软件 trigger 取消；窗口外电平变化会刷新边沿状态但不会记录位置；如果 Servo 提供锁存位置，`MC_TouchProbe` 会用该锁存位置做窗口判断并写入 `RecordedPosition`；未 armed 或不匹配的 trigger 取消请求按幂等成功处理，unsupported trigger input 会返回 `PARAMETER_NOT_SUPPORT`。
- `KB-005`：`MC_DigitalCamSwitch` 使用调用方持有的定长 `CamSwitchTable<8>`，将 `TrackNumber` 0..3 映射到 `AxisModel` 四通道软件数字输出；支持同轨多窗口 OR 与可选周期窗口跨界，并在换表、换轴、禁用或错误时清理旧受控轨道。E 级 time mode、Duration、AxisDirection、Compensation、Hysteresis 与硬件 compare offload 不在当前实现范围。
- `KB-006`：`MC_ReadParameter` / `MC_WriteParameter` 当前按显式 supported parameter registry 覆盖位置、软限位、速度、加速度/减速度、jerk 配置值、跟随误差限制和跟随误差监控状态；`MC_ReadBoolParameter` / `MC_WriteBoolParameter` 支持软限位开关和跟随误差监控开关；注册表外 PLCopen/vendor 参数返回 `PARAMETER_NOT_SUPPORT`；system/application 速度限制共享同一个轴速度极限，加速度/减速度限制共享同一个轴加速度极限，system/application jerk 共享同一个轴 jerk 配置值；该 jerk 配置值当前仅提供参数存取与合法性校验，不参与统一运行时 jerk 限幅。
- `KB-007`：`MC_ReadAxisInfo` 当前报告仿真、Servo communication ready、Servo ready for power on、power、homed、软件限位越界状态，并通过 `MC_SERVO_EXTENSION_DIGITAL_INPUT_BASE + 0/1/2/3` 读取 home switch、正限位、负限位和 axis warning；`AxisWarning` 也会读取 Servo warning hook。
- `KB-008`：`MC_MoveSuperimposed` 当前以独立 superimposed offset 轨迹叠加到 base motion；`MC_HaltSuperimposed` 只停止这条叠加轨迹，不中断 base motion。
- `KB-009`：`MC_MoveVelocity` 当前支持 `Direction` 符号选择，并支持 `ContinuousUpdate = TRUE` 时在 `Execute` 保持为真期间更新目标速度和方向；`MC_MoveContinuousAbsolute` / `MC_MoveContinuousRelative` 当前支持到达目标后保持非零结束速度、active 命令重规划连续位置目标、disabled-update 边界和 override 重规划，其中 relative 更新按本次命令触发时的起点重新计算目标距离；`MC_PositionProfile` / `MC_VelocityProfile` / `MC_AccelerationProfile` 当前支持 active 当前段的命令更新；`MC_GearIn`、`MC_GearInPos`、`MC_CamIn` 和 `MC_CombineAxes` 支持 active 同步参数的 `ContinuousUpdate`。
- `KB-010`：`MC_PositionProfile` / `MC_VelocityProfile` / `MC_AccelerationProfile` 当前支持通过 profile data 的 `mNext` 指针顺序消费链式多段 profile，并在进入当前段命令前应用各自的 timed segment、time scale、value scale 与 offset 输入；`MC_VelocityProfile` 与 `MC_AccelerationProfile` 都复用当前速度运动路径，尚不包含标准 profile table 解析、时间戳/延迟消费或复杂插补。
- `KB-011`：`MC_TorqueControl` 当前把扭矩设定直接透传给伺服抽象，执行成功后置 `InTorque`，`Execute` 拉低时清零扭矩设定；更深入的 torque-mode 闭环控制仍取决于具体伺服实现。
- `KB-012`：`AxesGroup` 除 foundation 生命周期外，`v0.11.0` 支持 ACS 下 2-8 轴 `MC_MoveLinearAbsolute` / `MC_MoveLinearRelative` 的共享标量路径、Aborting/Buffered、CommandID、成员共同限制、GroupStop 与错误传播；`MC_GroupStop` 按当前路径和 Deceleration/Jerk 受控减速，速度为零后置 Done，并在 Execute 保持为真时继续保持 GroupStopping。当前不支持非 ACS 坐标系、kinematics、Cartesian pose、几何 transition、圆弧、look-ahead 或硬件级同步。`MC_GroupReadActualPosition` / `MC_GroupReadCommandPosition` 仍读取 0-based 成员槽位，不是 Cartesian group pose。
- `KB-013`：`MC_Gear* / MC_Cam*` 当前是单主单从同步实现，要求主从轴位于同一个已启用的 `AxesGroup` 中；`MC_GearIn`、`MC_GearInPos` 和 `MC_CamIn` 支持 `MasterValueSource` 的 command/actual 两种采样来源，并支持 active 期间的 `ContinuousUpdate`；`MC_GearInPos` 支持 `MasterStartDistance`，会在接近窗口内按线性或 profiled 轨迹把从轴带到 `SlaveSyncPosition`，再按 `SlaveSyncPosition - MasterSyncPosition * ratio` 建立相位偏移并进入同步；扩展 `SyncMode` 属于未来同步模型扩展。
- `KB-014`：`MC_PhasingAbsolute` / `MC_PhasingRelative` 在 `Velocity > 0` 时按速度、加速度、减速度和 jerk 规划 phase offset 过渡；`Velocity = 0` 保留直接调整语义；目标和 profile 输入在当前 execute 周期内锁存，执行中输入变化需重新触发后才会影响下一次命令。
- `KB-015`：`MC_CombineAxes` 当前是 tested two-master setpoint combination 块，会按 add/sub 模式、每路 gear ratio 和 master value source 生成 slave setpoint；坐标系、kinematics 和 Part 4 路径合成仍不在范围内。
- `KB-016`：`MC_CamTableSelect` 当前负责表校验和句柄传递，要求 `CamTable` 非空、master/slave 点有限且 master 点严格递增；`MC_CamIn` 直接消费选定的 `CamTable` 句柄并复用同一校验；`CamTable` 支持显式 opt-in 的周期采样；`MC_CamIn` 支持 master/slave offset 与 scaling，可在 `MasterStartDistance > 0` 时按线性轨迹接近 `MasterSyncPosition` 处的凸轮目标；尚不支持标准 profile-table 解析、独立控制器侧表仓库或速度/加速度/jerk 限制的接入轨迹。
- `KB-017`：`mStartSync` 当前建模为 `MC_GearInPos` / `MC_CamIn` 接近窗口启动和同步进入时的一拍脉冲。
- `KB-018`：`pyplcopen` 当前只暴露新核单轴 smoke facade，不是完整 Python PLCopen SDK。

以下条目描述新核 `core/` 相对 v0.x 行为的**已声明边界**（迁移背景见
[doc/migration-v0-to-v1.md](../migration-v0-to-v1.md)；对应旧口径条目在括号中注明）：

- `KB-019`：新核中同步（gear/cam/combine）从轴只接受 aborting 命令接管，非 aborting 运动命令显式报 `invalid_argument`（同步无定义完成点，旧线为排队等待）；组级 `stop` 不中止成员级同步（组命令与成员同步分属两个写者），`disable`、从轴 aborting 命令或 `sync_out` 会解除；同步接入与 aborting 基础命令都会清除运行中的叠加偏移。
- `KB-020`：新核 `MC_SetOverride` 承接 `KB-003` 的 active 重规划合同，接口已在 P1-A2 对齐为 Enable/Enabled + VelFactor `[0,1]`：velocity 命令逐周期响应；active 离散/homing/连续剖面按新限重规划；factor=0 受控减速暂停且保持原状态/命令，恢复正值继续目标；失败原子保留旧 factor/剖面。同步节点仍由 master value 驱动，不作本地 override 重规划。
- `KB-021`：新核同步逼近段（`MasterStartDistance` 窗口）按主轴行程线性插值并支持可选每周期速度上限，不建模加速度/加加速度整形的接入轨迹（修订 `KB-013` / `KB-016` 的 profiled approach）；相位过渡（`MC_Phasing*`）为速度斜坡，加速度/减速度/jerk 输入不再暴露（修订 `KB-014`）。
- `KB-022`：新核 `MC_TouchProbe` 触发源为 `AxisModel` 固定 4 通道数字输入组（`set_digital_input`），记录 capture 周期的 actual position，不承接 Servo 锁存位置回读（修订 `KB-004`）。
- `KB-023`：新核参数注册表不建模位置滞后监控（`ENABLE_POS_LAG_MONITORING` / `MAX_POSITION_LAG` 显式报 `unsupported`，修订 `KB-006`）；错误码粗映射：`PARAMETER_NOT_SUPPORT` → `unsupported`，`AXIS_GROUP_MISMATCH` / `GROUP_DISABLED` → `precondition_failed`。
- `KB-024`：新核轨迹表为调用方持有的定长段数组（≤8 段，替代 `mNext` 链表），段时长为周期计数且 `TimeScale` 作用于时长；三类 Profile 均在提交首段前预校验缩放时长的可表示性：PositionProfile 的正时长与 Velocity/AccelerationProfile 的非终段时长取整后必须至少为 1 周期且可表示为 `int64_t`，速度/加速度剖面终段无限保持；Velocity/AccelerationProfile 还对缩放/偏移后的目标速度、加速度/减速度和 jerk 做整表静态原子校验，静态非法后继不会产生部分执行；这不构成 PositionProfile 规划可行性的批事务保证；PositionProfile 仅在最后命令 ID 已完成时报告 Done，表内 active/pending 命令保持 Busy，被接管时报 CommandAborted；`ContinuousUpdate` 仅支持单段剖面（修订 `KB-010`）。
- `KB-025`：新核 `MC_MoveContinuous*` 与速度/加速度剖面的 `Done` 表示"保持终速中"的持续状态而非锁存完成态，被接管时报 `CommandAborted`；`MC_HaltSuperimposed` 当周期完成，不建模叠加偏移的减速段（补充 `KB-008`）。
- `KB-026`：新核离散运动（含叠加偏移与连续运动的规划段）经近时间最优 7 段 jerk-limited S 曲线求解器规划（`otg::plan_time_optimal`）：同等约束下运动时长显著缩短（零初始加速度状态域总时长约为原保守 quintic 求解器的 65%），加速度形状由平滑多项式变为梯形/三角相位；包络与端点承诺不变，回放基线升级为 `core-single-axis-move-v2`。aborting 接管现承接当前命令加速度（接管处加速度连续）；若新命令的限位容不下当前状态（如更小的加速度限位），接管显式报 infeasible 而非假装加速度为零。
- `KB-027`：组线性命令的共享标量路径参数由 jerk-limited 1D 剖面驱动（此前新核为恒速线性插值），`Acceleration`/`Deceleration`/`Jerk` 输入生效，动力学以行程最长成员为基准；成员共线性由单一路径参数构造保证不变；回放基线升级为 `core-group-linear-v2`。`MC_GroupStop` 按 `Deceleration`/`Jerk` 沿原路径受控减速（刹车距离超出剩余路径时在命令终点停住），停车清空命令队列；对成员级 gear/cam 同步仍不生效（`KB-019`）。
- `KB-028`：单轴 `MC_Halt`/`MC_Stop` 按命令 `Deceleration`/`Jerk` 从接管速度受控减速到静止（承接 v0.x 合同；此前新核为一周期即停），刹车目标豁免软限位检查（轴必须被允许停下）；aborting 接管全面保持运动学连续（新命令从被接管时的真实速度/加速度规划，此前 abort 会把速度瞬移为零再规划），速度限位低于当前速度的接管按"减速进入新包络"规划而非拒绝；回放基线升级为 `core-velocity-stop-v2`。
- `KB-029`：新核承接 `KB-001` 的单轴速度阈值 blending：后继命令为 `BLENDING_LOW`/`BLENDING_HIGH` 时，前一 `move_absolute` 剖面速度先超过再跌破标称速度的 30%/70% 阈值即提前交接（后继从实时状态规划，前命令在交接点报 `Done`）；未达阈值的短行程退化为 `BUFFERED`；homing、halt/stop 与连续保持不参与 blending。快照新增 `last_completed_command_id` 与 `command_pending` 查询：buffered/blending 链中已完成的前命令报 `Done`、排队中的后继报 `Busy`（修复此前二者都被误报 `CommandAborted` 的观察缺陷）。
- `KB-030`：新核组圆弧运动 v1（`MC_MoveCircularAbsolute/Relative`，A3）：仅三点 BORDER 模式（CENTER/RADIUS 显式 `unsupported`）、仅 ACS、2-8 轴（前两轴张成圆弧平面，其余轴线性跟随路径参数）、Aborting/Buffered（blending 归 A4，显式 `unsupported`）；路径参数为平面弧长，`Velocity` 等动力学输入作用于平面路径速度（linear 仍以行程最长成员为基准）；退化几何（共线、点重合、整圆、曲率半径超过弦长×1e6 的病态弧、非有限/维数不匹配）全部显式 `invalid_argument`，PathChoice 与三点确定的弧向不一致显式报错；验收规格见 `doc/compliance/plcopen-motion-part4-circular-matrix.md`，回放黄金场景 `core-group-circular`。
- `KB-031`：新核组几何 blending v1（A4）：`MC_MoveLinear*` 后继以 `mcTMMaxCornerDeviation` + `BLENDING_LOW/HIGH` 请求公差带拐角过渡（五次 Bézier，端点零曲率，与直线段 C2 连接），接受后整链融合为单一欧氏弧长路径由单一 jerk-limited 剖面驱动，链上限速取两命令限速与曲率限速（√(a/κmax)）的最小值、加/减速与 jerk 取两命令包络最小值；**构造性节拍门槛**：链时长优于"完全停止 buffered 基线"才提交，否则连同反折拐角（对齐 < -0.999）、过迟提交（已进入过渡区）一起显式降级为 `BUFFERED`（`last_blend_degraded_command()` 可查，不静默）；共线后继直通不降速、无过渡曲线；公差 ≥ 相邻段长一半时过渡区截断到段长一半（偏差只缩不涨）。v1 边界：仅 linear→linear、仅作用于"活动命令 + 空队列"，已提交链不可再扩展（后续 buffered/blending 显式 `unsupported`）；blending 链内速度语义为欧氏路径速度（与 `KB-027` 最长成员基准不同，与 `KB-030` 圆弧一致）；组 GroupStop 沿链几何受控停车（可停在过渡区内）；v0.x 单轴 30%/70% 阈值语义不迁入组路径（`KB-029` 为单轴口径）。验收规格 `doc/compliance/part4-blending-semantics.md`，回放黄金场景 `core-group-blend`。
- `KB-032`：新核 look-ahead v1（A5）：连续 blending 后继构成前瞻窗口（容量 64 段，满报 `capacity_exceeded`），结点速度 = min(两命令限速, 过渡曲线曲率限速, 梯形级双向扫描可达速度)，每段独立 jerk-limited 剖面按结点速度衔接（经 OTG vt≠0 巡航域精化候选）——**声明变更**：blending 链执行从 KB-031 整链单剖面（链上限速）升级为分段结点限速，直线段不再被拐角限速拖慢，回放基线升级 `core-group-blend-v2`；密集 16 段折线实测节拍 ≈ 完全停止基线的 69%。窗口提交时同步重算（周期路径零规划），几何上限冻结、已承诺件不回撤、未开始件全量重规划；`BUFFERED`+`mcTMNone` 仍完全停止、`Aborting` 仍即时接管；plain buffered 可排在窗口后（窗口以静止终结），buffered 队列非空时 blending 显式 `unsupported`；稠密极限（公差截断吃光段线长）与反折、过迟提交、不优于停车基线一样显式降级；GroupStop 沿已承诺窗口几何复合弧长受控停车。验收规格 `doc/compliance/part4-lookahead-semantics.md`。
- `KB-033`：新核 look-ahead v2（A5 v2）：KB-030 BORDER 圆弧段可经 `BLENDING_LOW/HIGH` + `mcTMNone` 加入前瞻窗口——直通性由 N 维切向连续判定（对齐 > 0.999 直通不停车；不对齐显式降级为 BUFFERED 停止衔接，`last_blend_degraded_command()` 可查）；圆弧段整段限速 min(命令速度, √(a·R))（向心约束）；圆弧段不修剪，线弧公差带过渡曲线留 v3（`mcTMMaxCornerDeviation` + 圆弧显式 `unsupported`）；活动圆弧命令不做窗口种子（显式 `unsupported`，窗口只由活动线性命令转换生成）；螺旋高轴线性跟随与 GroupStop 复合弧长停车口径沿用。回放黄金场景 `core-group-window-arc`。
- `KB-034`：OTG 求解器鼓包区修复（**声明变更**）：巡航速度选择改为按直达 ramp 距离选单调分支再二分（链距离 D(vc) 在两边界速度之间因 ramp 拆分开销非单调，旧全域二分可收敛到伪交点——短前向运动选出负巡航速度、快候选全部退化）；新增估计锚定单段 quintic 候选（以连续时间链时长为下界向上探测，支持非零入口加速度，同向边界形态卫兵拒绝反向摆动），鼓包区（直达距离 < 目标距离 < vmax 链距离）不再落入残差燃烧修正（B9 跟踪 case 规划 68 → 19 周期）。回放基线 `core-group-window-arc` 升级：端点逐位不变，时长 90 → 88 tick；其余 fixture 逐位不变。求解器自身合同仍允许 overshoot-and-return（折返排除由跟踪器侧负责）。测试：`otg_time_optimal_tests` 鼓包区固定 case + 随机层。
- `KB-035`：新核 B9 轨迹流滤波第一片（`stream::StreamFilter1D`，L3）：单关节带时间戳目标流（keep-latest，时间戳严格递增，乱序拒绝并计数）经事件驱动 OTG 在线重解升频到插补周期，速度/加速度/jerk 包络构造性满足；可选位置包络先钳位输入目标并置标志，再在归一化时间域验证候选剖面的完整段内部：constant-jerk 段检查端点与全部速度零点，quintic 段通过加速度零点处的速度极值证明单调性，数值不安全或非单调候选进入求解器回退或保留上一安全剖面，而非对周期输出事后裁剪；两级断流看门狗（输出速度线性衰减外推 → jerk-limited 受控停，新目标自动恢复跟踪且接管连续）；运动目标跟踪律为"前方一个自适应视界的线点 + 并线下限"，优先使用 `solve_fixed_time` 定时到达视界点（rendezvous 剖面将动力学均匀分布于视界窗，不足时回退 `plan_time_optimal`——声明变更），锁定为精确线性骑行、稳态偏移 ≤2 插补周期（整周期量子平台边界）；输出流永不中断（候选依次经过快路径、定时、time-optimal、静止目标与 baseline 回退，全链失败才保持上一剖面并计数）。验收规格 `doc/compliance/trajectory-stream-semantics.md`，测试 `plcopen_core_stream_tests`。轴级流会话已落地（`stream_engage/push/disengage`：Aborting 类接管进入、运动中 engage 走受控停梯子、标准 FB Aborting 接管退出且连续、非 aborting/同步/叠加命令显式拒绝、disengage 仅静止可用、会话状态呈现 `synchronized_motion`、轴软件位置限位不自动并入流包络；测试 `plcopen_core_stream_session_tests`）。多关节聚合 `stream::JointStreamGroup`（≤32 关节共享配置、逐关节独立滤波，不承诺关节间时间同步）；28 关节 @1kHz 预算微基准：交错 100Hz 稳态 ~24µs/周期、全关节每周期重解 ~241µs/周期（< 300µs 预算门槛，`STREAM_METRICS` 进基准趋势）；pyplcopen 流接口（`stream_engage/push/disengage/now/mode`）与 smoke demo；回放黄金场景 `core-stream-session`（跟踪-断流-恢复-Aborting 接管全链）。
- `KB-036`：新核 B1 坐标系栈 v1（已批准坐标矩阵 `doc/compliance/part4-coordinate-semantics.md`）：组命令目标可在 `MCS`/`PCS` 笛卡尔帧表达（`GroupCommand.coord_system`，默认 ACS 全向后兼容；`WCS/FCS/TCS` 显式 `unsupported`）；帧换算全部前置到 submit（周期路径零新增计算，blending/前瞻窗口天然混帧安全）；ACS↔MCS v1 为声明的恒等映射（笛卡尔正交轴组，kinematics 插件属 B2）；PCS = 工件帧（平移 + 绕 Z 单轴旋转，完整 RPY 留 v2）；工具偏置为 TCP 平移（绝对命令终点减偏置，相对命令只旋转不平移——偏置在两 TCP 位置间相消）；前 3 维参与笛卡尔语义、4-8 维按 ACS 直通；帧/偏置仅组 standby 且队列空可设置（运动中换帧无定义语义，显式拒绝）；PCS 未设帧 = 恒等（声明，不报错）。验收 `plcopen_core_coordinate_tests`（几何等价 oracle 逐周期 1e-9），回放黄金场景 `core-group-pcs`，既有基线逐位不变。
- `KB-037`：新核 B2 kinematics 插件 v1（已批准矩阵 `doc/compliance/kinematics-plugin-semantics.md`）：`kin::Kinematics` 头文件 ABI（跨 DSO 留 Phase C）+ 合规 harness（往返 fuzz / seed 分支稳定 / 确定性），参考实现龙门与 SCARA（全解析，肘部分支随 seed 不跳支，θ1 向 seed 归一）；`AxisGroup::set_kinematics` 后 MCS/PCS 命令经 帧栈→工具偏置→逆解（seed=段起点） 落 ACS 关节目标，奇异 margin 禁入区预检查（违例 `precondition_failed`），ACS 命令直通；**v1 声明边界**：逆解仅作用于端点与 aux 点，段内插补仍为关节空间参数化（非线性构型下 MCS 直线 ≠ 笛卡尔直线；逐周期笛卡尔插补 + 双空间限速属后续批次），joint==cartesian==组轴数（6R 批次解除）。验收 `plcopen_core_kinematics_tests`；既有回放基线逐位不变。
- `KB-038`：新核 B4 cam 高阶化 v1（已批准矩阵 `doc/compliance/cam-curve-semantics.md`）：`CamInCommand.interpolation ∈ {linear(默认，逐字节兼容), spline}`——spline 为 engage 时一次性重建的 C2 三次样条（非周期自然边界、周期表首尾位置/速度/加速度全连续；表点数 ≤64，错位周期表显式拒绝），周期路径只加多项式求值；`cam_switch` 在 engaged 态同主轴在线换表，切换点从轴位置连续（超差 `invalid_argument`），速度/加速度受限跳变显式声明（OTG 平滑桥接留 v2）。同表同主轴实测样条从轴加速度冲击 < C0 的 1/3。验收 `plcopen_core_cam_tests`；linear 模式与既有回放逐位不变。
- `KB-039`：新核 look-ahead v2 jerk 修正（已批准 `doc/compliance/part4-lookahead-v2-jerk.md`）：窗口双向扫描的结点可达速度从梯形估算（`√(v²+2as)`）升级为精确 jerk 受限可达解（`plan::jerk_reachable_speed`，梯形值上界的有界二分，submit 时求解）；构造性单调收紧（修正只降不升）。离线修正因子表方案显式放弃（为在线重算准备，本架构用不上）。既有回放语料逐位不变（既有场景结点由拐角上限主导）；修正在 jerk 主导域生效。
- `KB-040`：新核 B5 纯软件面（ADR-0004 Accepted）：`core/adapters/`——`Servo` 稳定窄接口（全阶前馈 setpoints + 反馈/数字输入/诊断位）、executor 侧桥接（核心 L5 不持有 Servo 指针，值语义/确定性回放/不碰 OS 不变，双胞胎命令域逐周期等价为证）、`ServoSim` 理想驱动（仿真↔真机零修改承诺的载体）、CiA402/DS402 状态机（纯软件全梯形：上电阶梯/quick stop/故障反应/复位，无总线 I/O）、CSP/CSV/CST 模式管理骨架（bumpless：切换瞬间新主通道从锁存全阶状态接续）。真栈对接/DC 对齐/模式握手属 B5 真驱动语境（plcopen-fieldbus 仓库，硬件延后清单）。验收 `plcopen_core_adapters_tests`。
- `KB-041`：新核 BS3.5/BS3.6 收口：球腕 6R 解析逆解（`kin::SphericalWrist6R`，Pieper 腕心分解 + ZYZ 手腕，8 分支 seed 最近选支、max_joint_step 不跳支门、腕/肘/肩奇异 margin；预集成 Pose6 形态，组接线随姿态批次）+ 组级双空间限速（`set_cartesian_velocity_limit`：submit 时关节弦正解采样保守缩放，v1 线性段）。验收 `plcopen_core_wrist6r_tests`（2 万例往返 fuzz ≤1e-8）。
- `KB-042`：新核姿态批次收口（已批准矩阵 `doc/compliance/orientation-semantics.md`）：6 关节组经 `set_pose_kinematics` 启用位姿管线（与平移插件互斥，组轴数 == joint_count == 6）——组命令槽位 [0..2] = 位置、[3..5] = RPY（外旋 X-Y-Z，R = Rz(yaw)·Ry(pitch)·Rx(roll)），仅 submit_linear + 绝对 + MCS/PCS；PCS 左乘完整刚体工件帧（`set_workpiece_frame_rpy`，绕 Z 旧口径保留为其特例）、右乘工具逆变换（`set_tool_transform_rpy`，法兰→TCP；平移管线的 `set_tool_offset` 与之互不读取），`PoseKinematics::inverse`（seed = 段起点关节，KB-041 不跳支门 + margin 端点预检查）落 6 关节 ACS 目标走既有共享路径。段内仍为关节直线插补（姿态测地/笛卡尔直线属笛卡尔插补批次）；relative/circular/blending 过渡显式 `unsupported`（circular 在 `submit_circular` 入口守卫，不依赖 path_kind 旗标）；ACS 命令直通。验收 `plcopen_core_pose_tests`（管线等价 oracle 逐周期 ≤1e-9、端到端 forward 位姿 ≤1e-8、TCP 复合、拒绝矩阵全表、旧绕 Z 工件帧特例逐周期回归）；既有回放语料逐位不变。
- `KB-043`：新核回读批次收口（已批准矩阵 `doc/compliance/readback-semantics.md`）：`AxisGroup::read_cartesian(cs, source, out[, gimbal])` 按帧（ACS/MCS/PCS）读组命令位/实际位，纯 const 查询、输出布局与提交侧逐槽位镜像（读回即可重提交）；位姿组输出 TCP 位姿（[0..2] 位置 + [3..5] RPY），平移组输出 TCP 点 + 高维 ACS 直通。矩阵→RPY 反演唯一入口 `geom::extract_rpy`：pitch ∈ [-π/2, π/2]，万向节带（hypot(r00,r10) < 1e-9）按约定 roll=0 全部折入 yaw 并置标志（不报错不静默）；RPY 跨周期连续性/解卷绕声明不承诺。帧配置回读原值回显（setter 缓存 RPY 六元组，不反演矩阵）。组读 FB 增 `coord_system` 输入（缺省 ACS 逐字节兼容）+ `gimbal_lock` 输出。WCS/FCS/TCS `unsupported`、空/禁用组 `invalid_argument`。验收 `plcopen_core_readback_tests`（10 万例 RPY 反演重建 oracle ≤1e-12 + 万向节带定向采样 ≤1e-9、位姿/平移/插件组往返、配置回显逐位、FB 兼容护栏）；周期路径零改动，既有回放逐位不变。
- `KB-044`：新核笛卡尔插补批次收口（已批准矩阵 `doc/compliance/cartesian-interpolation-semantics.md`）：`GroupCommand.interpolation_space ∈ {joint(默认，逐字节兼容), cartesian}`——kinematics/位姿组的 MCS/PCS 绝对直线段 opt-in 段内逐周期解析逆解：TCP 位置真笛卡尔直线、姿态测地旋转（submit 预计算轴角 + 周期 Rodrigues；θ ≥ π−1e-6 显式 `invalid_argument`），1D jerk 剖面直接驱动笛卡尔弧长（纯旋转段由角驱动），`cartesian_velocity_limit` 直接生效（取代 BS3.6 保守缩放）。submit 33 采样 seed 链预验证（不可达/越步门 → `infeasible`、margin → `precondition_failed`）；位姿组命令速度按最坏采样比缩放使每周期关节步 ≤ 0.5×max_joint_step（step 门兼作关节速度预算；平移 v1 ABI 无步门参数，由 margin 禁入区约束——声明）。采样间周期内失败 = 组 errorstop（`last_cartesian_error()`，保持上周期 setpoint，不外插不跳支）。接管沿用既有组语义：几何从当前状态连续、路径剖面从静止重规划（矩阵决策 #8 的入口速度映射降格为该口径，与 joint 模式组接管一致——声明）。relative/circular/blending/ACS/恒等组显式 `unsupported`；笛卡尔段不进 look-ahead 窗口。验收 `plcopen_core_cartesian_tests`（SCARA 线性度 1e-8 + 关节弯曲对照、6R 独立四元数 slerp oracle 1e-8、关节步预算全周期断言、mock 采样间失败注入、跨模式接管连续、拒绝全表）；回放新黄金场景 `core-group-cartesian`、既有语料逐位不变；预算基准 `cartesian_ik_cycle_us` 实测 2.4µs（Debug 上限口径）≪ 50µs 硬门。
- `KB-045`：腕奇异通过（已批准笛卡尔 v2-A 增补）：`SphericalWrist6R` 腕奇异带从精确零（1e-12）拓宽——带内 ZYZ 不定度按 q4 = seed 同圈锁定、q6 承接剩余旋转；带阈实现取 **1e-8**（较批准案 1e-6 收紧：约定姿态误差 ~d6·π·带阈 需低于 1e-8 线性度门槛——声明）。带外解析解逐位不变（KB-041 fuzz 原样），肘/肩禁入不变。**声明**：穿越腕奇异的重定向拍以 max_joint_step 门为界（KB-044 决策 #6 的 0.5 预算不覆盖它——该跳变集中于零测度参数区间，减速无法摊薄）。验收进 `plcopen_core_cartesian_tests`（穿越无 errorstop、线性度 1e-8、步 ≤ 门）。
- `KB-046`：cam 运动规律生成器（已批准 cam v2 增补）：`exec::cam_law_value/generate_cam_law`——摆线/修正正弦/3-4-5 静止-静止升程离散为 ≤64 点表（调用方存储，离线/提交域），修正正弦常数由分段原函数运行期推导（不抄手册）。验收进 `plcopen_core_cam_tests`（边界 oracle、数值 C2、单调、峰值比 ±1%：Cv 2.000/1.760/1.875、Ca 6.283/5.528/5.774，生成表过 engage 校验）。CSV 表导入为 pyplcopen 侧 helper（核不做文件 IO）。
- `KB-047`：笛卡尔圆弧（已批准笛卡尔 v2-B 增补）：`submit_circular` + `interpolation_space = cartesian`——三点 BORDER 弧在笛卡尔(TCP)空间 XY 平面成弧、z 沿参数线性（KB-030 平面约定移植；任意空间弧平面留后续——声明）；位姿组姿态沿弧扫角分数走起终测地，aux 姿态槽位忽略（声明）；预验证/预算/errorstop/GroupStop 沿用 KB-044 机制；退化几何/PathChoice 冲突口径与 KB-030 相同。验收进 `plcopen_core_cartesian_tests`（SCARA/6R 弧半径逐周期 ≤1e-8、姿态测地 oracle ≤1e-8、拒绝表）。
- `KB-048`：窗口深度运行期可配（已批准 lookahead v2 增补）：`set_window_depth(n∈[2,64])`（standby + 空队列 + 窗口未激活守卫）；达到配置值按既有 `capacity_exceeded` 拒绝扩展；默认 64 逐位兼容。验收进 `plcopen_core_a5_lookahead_tests`。
- `KB-049`：笛卡尔 blending（已批准笛卡尔 v2-C 增补）：活动笛卡尔直线段 + `blending_low/high` + `mcTMMaxCornerDeviation` 笛卡尔直线后继 → 笛卡尔(TCP)空间五次 Bezier 拐角融合为单弧长链、单剖面（从活动段实时路径状态接续规划）；链包络取两命令最小值，拐角曲率限速 √(a/κ) 作用于整链（KB-031 v1 口径——结点级限速属笛卡尔窗口批次）；姿态为全链起终单条测地（拐角处无姿态折点——声明）。构造性门槛（不优于停车基线）、反折拐角、过迟提交、公差吃尽全部降级 BUFFERED（`last_blend_degraded_command` 口径）；链不可扩展、joint↔cartesian 混模 blending、弧/纯旋转活动段全部显式 `unsupported`；不进 look-ahead 窗口。**急拐角在整链限速下常输给停车基线而正确降级**（构造性门槛的本意，声明）。验收进 `plcopen_core_cartesian_tests`（缓拐角融合不停车且全程在公差带、反折降级并完成、混模拒绝、链不可扩展、6R 全链测地轴一致性 + 终点姿态 1e-8）。
- `KB-050`：笛卡尔前瞻窗口（已批准笛卡尔 v3 增补）：平移插件组的连续笛卡尔 blending 后继构成 TCP 空间窗口（直线 + 五次拐角，v1 容量上限 16 段/32 件）；结点速度 = 双向 jerk 精确扫描 ∩ 拐角曲率限速 ∩ 命令限速 ∩ cartesian_velocity_limit；**所有非拐角件使用 OTG jerk 限剖面**（`plan_time_optimal` + `solve_fixed_time` 回退——若 OTG 最优时长因量化残差膨胀超 ideal+4 周期，扫描 `[ideal, ideal+4]` 区间的 `solve_fixed_time` 多段恒定 jerk 候选），不再使用匀速骑行或退避梯子（KB-056 定时求解使之多余——声明变更）。逐周期窗口采样 + 单次逆解（KB-044 机器），失败 = 组 errorstop；扩展预验证沿新段 33 采样 seed 链（拐角在公差球内，声明）；反折/过迟/拐角骑行中扩展降级 BUFFERED（报告）；GroupStop 沿复合几何受控停车；位姿组维持 KB-049 链（窗口化留 v2）。验收 `plcopen_core_cartesian_tests`（6 段折线 ≤0.8× 停车基线、90° 拐角不降级、全程公差带、停车在几何上、不可达扩展拒绝不扰窗口）；新黄金场景 `core-group-cartesian-window`（18 语料）；**声明变更**：匀速骑行与退避梯子删除致回放基线 `core-group-cartesian-window`/`core-group-cartesian`/`core-group-window-arc` 变更（剖面从常速段变为 jerk 限曲线）。
- `KB-051`：**已修复（Y7，2026-07-08）**：组级 linear `aborting` 接管现为速度连续——入口标量速度 ṡ₀ = ⟨q̇, t̂⟩（成员速度在新路径切向的投影），横向残差经独立 1D jerk/accel 受限剖面衰减至零（接管连接段）；连接段内动力学限值按 β=0.5 逐轴 L∞ 分割（沿路 + 横向叠加不超限）；横向偏差 ≤ R_tube（解析预计算、可回读）；连接段结束恢复严格路径语义。对齐接管（q̇ ∥ t̂）退化为零长连接段，第一拍即在线。静止接管语义不变。连接段内再次 aborting 可重入。验收 `plcopen_core_y7_group_takeover_tests`（9 场景：速度连续、前向承接、公差管、叠加不超限、对齐退化、静止退化、重入、负投影、连接段中 GroupStop）。语义矩阵见 `doc/compliance/group-takeover-semantics.md`。**适用范围仅 linear 组**；circular/笛卡尔扩展待曲率链式项前置条件满足后另批。
- `KB-052`：**已修复（2026-07-08 对抗性探测轮发现 → 加速度连续修复）**：组级 aborting 接管（KB-051）现保证**速度和加速度连续**——`capture_takeover_velocity` 扩展为同时采集加速度分量 `takeover_acceleration_[i] = along.acceleration * dir_i`，`plan_connector` 将其投影到新路径切向得 `a_s0 = ⟨q̈, t̂⟩` 并 clamp 到 β 分割加速度限后传入 OTG 作为初始加速度。实现与语义矩阵 v2.1 决策 #1 对齐。
- `KB-053`：**已修复（2026-07-08 对抗性探测轮发现 → 停车距离预检修复）**：组级 aborting 接管（KB-051）入口速度远超新命令速度限时，OTG 剖面在路径终点前耗尽减速距离致速度断崖。修复：`plan_connector` 中计算 `ramp_between(s_dot_0, 0, along_limits).distance`，若停车距离 > 路径长度 × 1.5 则拒绝 connector，回退到安全的零速起步路径。β 固定为 0.5（自适应 β 已尝试但引起测试回归，已回退）。
- `KB-054`：**已确认为声明边界（2026-07-08 对抗性探测轮发现 → 探针验证）**：组 linear 路径的路径参数归一化基于"最长单轴行程"而非欧几里得路径长度（`group.h` start() 中 `longest = max(|finish_i - start_i|)`）。因此 v_cmd / a_cmd / j_cmd 是最长轴的动力学限制，不是欧几里得路径速度。N 轴等距对角线上，Euclidean 速度/加速度 = √N × 单轴值：实测 2 轴 1.29×、4 轴 1.82×（含 OTG jerk 量化调低系数 0.91）。PLCopen Part 4 定义 Velocity 为路径速度，当前实现在 ACS 坐标下按逐轴限制运行（与 KB-012 ACS-only 声明一致）。修复方向：将 `longest` 改为欧几里得距离 `sqrt(Σ delta_i²)` 可使 v_cmd 语义与 Part 4 一致，但属声明行为变更，需语义批准。极端 jerk/accel 比（100:1）不是根因——OTG 精确模式正确调低 jerk 使单轴峰值加速度在限内（0.91× a_max）。
- `KB-055`：OTG 非零目标加速度（Y2 §1，A9 v2）：`plan_time_optimal` 现接受任意目标加速度 a₁ ∈ [-d_max, a_max]（此前报 `unsupported`，修订 KB-026 声明）。实现为**目标斜坡**（targeting ramp）：n_t = ceil(|a₁|/j_max) 周期的恒定 jerk j_t = a₁/n_t 追加于主链之后，主求解器规划到有效目标 `(p_eff, v_eff, 0)` 其中 v_eff = v₁ - 0.5·a₁·n_t、p_eff = p₁ - v_eff·n_t - a₁·n_t²/6。当 |v_eff| > v_max（大 |a₁| 叠加近限速度）时，目标斜坡省略，quintic 校正直接处理非零边界加速度（grow-from-estimate 搜索替代不安全的二分）。超限目标加速度（a₁ > a_max 或 a₁ < -d_max）报 `infeasible`。验收：12 固定用例（rest-to-accel/decel、accel-to-decel、matching、reverse、边界、a0-and-at、zero-distance）+ 50k 随机 fuzz（包络 + 端点 + 连续性）+ oracle 两新域（nonzero-at max=20 avg=3.32、a0-and-at max=21 avg=4.40，hard_gate=false——目标斜坡用调整 jerk 而非 PMP {±j,0}）。

- `KB-056`：OTG 定时求解（Y4 §2，A9 v2）：新增 `solve_fixed_time(from, to, limits, total_cycles)` 一等原语，给定总周期数返回恰好该时长的 jerk 限 Profile1D。当 total_cycles == T_min（时间最优解时长）返回最优解；< T_min 时自包含候选（quintic、multi-cubic、quintic-cubic hybrid）仍尝试——多段恒定 jerk 候选的量化行为优于单段 quintic，可在 quintic T_min 以下找到有效剖面（声明）；全部候选失败才报 `infeasible`。八候选族：① 单段 quintic（大 T 场景）；①a 最优分裂——在最优剖面中间取采样点、构建双 quintic（短剖面 + 非零 a0/at）；①c 零速插入——扫描最优剖面段界的 v=0,a=0 点、插入空闲周期（速度反转剖面的关键路径）；①b 内 quintic（zeroing+quintic+targeting 三段）；①b' 零化斜坡 + 直接 quintic（无 targeting 斜坡简化路径）；①d 3-cubic 整数分相——3 段恒定 jerk 相位、Cramer 3×3 线性求解、解析极值检验（短剖面 quintic 振荡过大时的替代）；①e 4-cubic j₁ 扫描——4 段恒定 jerk（首段扫描 j₁ ∈ [-j_max, j_max] 步长 0.01、余 3 段 3-cubic 求解），覆盖 3-cubic 加速度超限的困难形态；①f quintic-cubic 混合——从目标反向传播恒定 jerk 尾段得中间点、正向 quintic 到中间点（解决 target < 4 无法 4-cubic 但 3-cubic jerk 微超限的边界）；②+③ 多相链（正/反方向巡航二分 + cruise 重试循环）。验收：17 固定用例（含 8 短剖面回归：3-cubic ×6、4-cubic ×1、hybrid ×1）+ 1000 tight fuzz（extra=1 专项）+ 1000 随机 fuzz（extra 1-30 周期，全状态随机含非零目标加速度、速度反转），T43 终态精度 worst ep=7.99e-15 ev=6.66e-16 ea=0。

- `KB-058`：GroupSetOverride factor=0 暂停语义（Part 4 管理 §GroupSetOverride）：语义矩阵声明 factor ∈ [0,1]，factor=0 等效暂停（位置冻结、组保持 moving）。探测轮 D2-OverrideZeroPause 发现原实现以开区间 (0,1] 校验——factor=0 被拒绝。修复：校验改为 factor < 0.0（闭区间下界）；factor=0 时计算制动停止位置（复用 GroupStop 的 brake_shift + ramp_between 模式），规划制动剖面，组保持 moving 但 finish_active 不转 standby；恢复 factor>0 时从暂停位置重新规划至原终点。测试 check_group_override_factor_zero_equivalent 验证暂停冻结 + 恢复到终点精度 ≤ 1e-9。

- `KB-057`：OTG 段时长 epsilon 政策（Y2 §2，声明项）：求解器工作在离散周期域——段时长 t_i 为非负整数（周期计数），连续时间候选解中 t_i < 0 的段按以下规则处理：|t_i| ≤ ε_clamp（实现取 1e-12，即远小于一个周期的数值噪声）时钳零（ceil(0)=0 周期）并**复验钳零后的边界条件**（位置/速度/加速度端点精度 ≤ 1e-9）；|t_i| > ε_clamp 的负时长候选整体丢弃。整数量化本身构成天然钳位（ceil 将 (0, 1) 区间的正连续时长映射为 1 周期），因此 epsilon 政策仅作用于结构切换流形附近的数值噪声域。oracle fuzz 在结构切换流形附近定向加密验证（两结构最优时长近相等的状态族）。该政策为算法合同 §1 附注 #2 的形式化登记。

- `KB-059`：几何路径导数合同（Y3 前置工程）：为 5 种段类型（LineSegment、ArcSegment、CubicBezierSegment、QuadraticBlendSegment、QuinticBlendSegment）实现 `path_derivative(seg, s)` 返回弧长参数化一阶导 q_s = dq/ds（单位切矢）与 `path_second_derivative(seg, s)` 返回二阶导 q_ss = d²q/ds²（曲率矢）。直线段 q_s 常数、q_ss = 0；圆弧段解析旋转导数；cubic/quadratic 以 u≈s/L 近似参数化后链式法则；quintic 通过 33 条弧长表重参数化、链式法则 q_s = d1/σ、q_ss = (d2 − d1·(d2·d1)/σ²)/σ²。PathSegment 类型擦除调度器同步更新。验收：7 项测试 × 中央差分数值 oracle（h₁=1e-7 一阶、h₂=1e-4 二阶）；圆弧曲率 = 1/R 精度 ≤ 1e-10；quintic 容差放宽至 q_s 2e-3、q_ss 1e-2（反映 33 条表离散化误差 O(1/N²)）。

- `KB-060`：TOPP-RA Layer 1 加速度限幅路径参数化（Y3 §3 首层）：实现 `solve_topp_ra(path, limits[3], grid_size)` 标量路径律求解器。算法：离散化路径 s ∈ [0,L]，逐格点计算最大速度曲线（MVC）含跨轴加速度可行性限制，后向可达性传播，前向最优积分，梯形时间合成。关键修正：cross_axis_velocity_limit 处理高曲率段不同轴的 q_ss/q_s 比率差异导致的加速度区间空集问题。验收：8 项 oracle 测试——直线梯形/三角剖面精度 ≤ 2%、加速度区间可行性验证（含跨轴）、曲率减速效应、网格收敛（N=50→800 相对差 < 0.5%）、非对称限值、cubic Bezier 可行性、速度下界 ∫ds/ṡ_max。声明：Layer 1 仅为加速度限幅——jerk 限制（Layer 2）和离散周期量化待后续批次。

- `KB-061`：几何路径三阶导数合同（Y3 前置工程完结）：为 5 种段类型实现 `path_third_derivative(seg, s)` 返回弧长参数化三阶导 q_sss = d³q/ds³。直线段/二次混合段 q_sss = 0（三阶参数导为零）；圆弧段 q_sss = {R·sin(θ)·ω³, -R·cos(θ)·ω³, 0}（解析旋转三阶）；cubic q_sss = 6·(p3−3p2+3p1−p0)/L³（常量——三次 Bernstein d³B/du³ 为常数）；quintic 通过 `quintic_third_derivative()` + 弧长重参数化链式法则 q_sss = [d3·σ² − 3·d2·α + d1·(4α²/σ² − β)] / σ⁵（α = d1·d2, β = |d2|² + d1·d3）。PathSegment 调度器同步更新。验收：8 项 oracle 测试——数值差分 d(q_ss)/ds（h=1e-5）、圆弧 |q_sss| = 1/R²（精度 ≤ 1e-10）、Frenet 恒等式 q_s·q_sss = −|q_ss|²、cubic 常数性、quadratic 零性。至此 q_s/q_ss/q_sss 完整交付，满足算法合同 §附注3 前置条件，Layer 2 jerk-aware TOPP 可启动。

- `KB-062`：TOPP-RA Layer 2 jerk-aware 路径参数化（Y3 §3 第二层）：实现 `solve_topp_ra_jerk(path, limits[3], grid_size)` jerk 限幅路径律求解器。约束：|q_sss_i·ṡ³ + 3·q_ss_i·ṡ·s̈ + q_s_i·s⃛| ≤ j_max_i（jerk 对 s⃛ 线性）。算法架构：(1) MVC 计算（复用 Layer 1 辅助）；(2) 正向 S-curve——从起点加速，ṡ=0 奇异用时域 S-curve 公式 t=(6·ds/j_eff)^{1/3} 启动，正常步用 jerk_interval.hi 最大加速，夹紧到 MVC；(3) 减速包络——在时间反转路径（q_s'=-q_s, q_ss'=q_ss, q_sss'=-q_sss）上从终点运行同构正向 S-curve，夹紧到 MVC；(4) 逐点取 min(x_fwd, x_dec) 做梯形时间积分。关键设计决策：放弃 2D (x,a) 后向可达性 + bisection 方案（MVC 过渡处 jerk 尖峰导致后向传播崩溃），改用对称正向 S-curve 取逐点极小值——速度连续、物理上等价于经典七段 S-curve 梯形律。验收：7 项 oracle 测试——T_jerk ≥ T_layer1（jerk 只能减速）、高 jerk 收敛到 Layer 1（j=1e6 差异 < 5%）、低 jerk 更慢（j=5 vs j=50）、圆弧 jerk ≥ Layer 1、网格收敛（N=50 vs N=400 差异 < 5%）、cubic Bezier jerk ≥ Layer 1、边界条件（grid_size=1 拒绝）。声明：Layer 2 为规划域影子求解器——离散周期执行器量化待后续批次集成。

- `KB-063`：TOPP-RA 影子 oracle——扫描基线对标（算法合同 §附注3 "先影子后换主"）：实现 `topp_shadow_oracle` 对 9 种场景（3 直线、3 圆弧、2 cubic Bezier、1 非对称限值直线）量化现行梯形扫描基线与 TOPP-RA L1/L2 的时间差距。基线 = 方向感知但曲率不感知的梯形律（中点切矢有效限值 v_eff/a_eff → 解析梯形时间）。scan_gap = (T_L1−T_trap)/T_L1：正值 = 扫描过于乐观（承诺不可达速度，安全隐患）；负值 = 扫描过于保守（浪费时间，TOPP 可优化）。关键发现：① 直线 gap≈0%（两者等价，验证正确性）；② 大弧/中弧 gap≈−10%（TOPP 利用 MVC 方向变化更快——扫描浪费 10% 时间）；③ 紧弧 R=0.5 gap=+9.8%（离心加速度限速，扫描忽略 → 承诺不可达速度）；④ S 形 cubic jerk_cost=+36.7%/紧弧 jerk_cost=+67%（高曲率变化段 jerk 开销显著）。结论：TOPP-RA 在大/中弧上比扫描更快，在紧弧上比扫描更安全——数据支持替换。门禁：直线 |gap|<3%、T_L2≥T_L1−ε、全解算器成功。

- `KB-064`：T24 quintic Hermite 解析快路径（算法合同 §4）：实现闭式五次 Hermite 插值 (p,v,a)→(p_aim,v_aim,0) over h 周期 + 解析极值校验。算法核心：归一化时间 σ=t/h∈[0,1]，q(σ)=Σd[k]σ^k，d[0..2] 由初始 (p,v,a) 决定，d[3..5] 由 6 边界条件解析求得（~30 flops）。极值校验：速度极值——q''(σ)=0 的三次根（Cardano 三分支：三实根/重根/单实根）+ 端点；加速度极值——q'''(σ)=0 的二次根 + 端点（非对称：max_acceleration/max_deceleration）；jerk 极值——q''''(σ)=0 的线性根 + 端点。容差 1+1e-9。关键修正：Cardano 求解器缺判别式为零的重根分支——当 r=half_q²+p³/27 在相对阈值内为零时，输出 simple root 2·cbrt(-half_q) 和 double root -cbrt(-half_q)。验收：9 项 oracle 测试——边界条件精度（6 条件 ≤ 1e-10）、Cardano 求解器（含重根/退化为二次）、Horner 求值一致性、known accept/reject、h=1 边界、解析 vs 稠密采样 fuzz（10000 用例 × 10000 采样点，零误接受）、非对称限值 fuzz（5000 用例，零误接受）、短视野 fuzz（5000 用例 h∈[1,5]，零误接受）。42/42 全套门禁通过。声明：算法核心就绪，集成到 StreamFilter 为配置开关（默认关）——待语义矩阵批准 + 回放口径声明后翻默认。

- `KB-065`：Y3 TOPP-RA executor 集成——规划域到执行域桥接（算法合同 §3）：实现 `topp_executor.h` 将 TOPP 规划域求解器（返回 optimal_time）桥接到 RT 执行域（需要整数周期 Profile1D）。管线：(1) TOPP solver → ToppVelocityProfile（sdot² 网格 + 最优时间）；(2) effective_scalar_limits（中点切矢投影，per-axis → 标量 1D 限值）；(3) max(ceil(T_topp), T_scalar_min) 量化（保证 solve_fixed_time 可行——TOPP L1 不含 jerk，标量 OTG 含 jerk，T_topp 可能 < T_scalar_min）；(4) solve_fixed_time → Profile1D；(5) verify_joint_limits 后验采样检查。两条管线：plan_topp_profiled（L1 加速度限幅）和 plan_topp_profiled_jerk（L2 jerk 感知）。关键设计决策：(a) 标量 Profile1D 按路径弧长均匀分配时间（曲率不感知），TOPP 的价值是全局最优时长；(b) jerk 缺省用 a²/(v·0.01) 启发式（纯加速度 TOPP 无 jerk 信息时的保守估计）；(c) T_scalar_min 下界保证 solve_fixed_time 不会因 jerk 约束被判 infeasible。验收：7 项测试——直线端点精度、弧线向心力减速（高 v_max 使向心力约束主导）、量化上界、执行域采样正确性、jerk 感知管线、速度剖面存储完整性、弧曲率后验检查。43/43 全套通过。声明：标量 Profile1D 分配时间均匀——高曲率段的实际轴速可能低于 TOPP 最优，安全但非最优；曲率感知的分段 Profile1D 为未来优化方向。

- `KB-066`：`check_cubic_phase_limits` 非对称加减速限值修复：`solve_fixed_time` 的 3-cubic/4-cubic 候选路径使用 `check_cubic_phase_limits` 做加速度限幅校验，但该函数将 `max_acceleration` 与 `max_deceleration` 取 `fmax` 合并为对称界——当两者不等时（如 accel=8, decel=4），允许减速段加速度 −7 通过校验（7 < 8），实际超出 decel 限值 4。修复：正加速查 `max_acceleration`、负加速查 `max_deceleration`，与 `within_limits` 函数一致。影响范围：`solve_fixed_time` 的短时段候选（total_cycles ≤ 20 的 3-cubic 与含预斜坡的 4-cubic）在非对称限值下可能输出超出减速限值的 Profile1D。修复后 43/43 测试通过（含 optimality oracle 随机非对称 fuzz）。

- `KB-067`：`set_group_override` 窗口零 override 恢复 NaN 修复：窗口运行时 `set_group_override(0.0)` 将所有后续段 `max_velocity` 乘零；再调 `set_group_override(f>0)` 时 `f/0 = inf`，`0 × inf = NaN`，窗口速度限值全部损坏。修复：`previous == 0` 时不做比例缩放，改用 `active_command_.velocity × factor` 重建速度限值；arc 段同步重算向心力速度上界 `min(v_cmd×factor, sqrt(min(accel,decel)×R))`，由后续 `window_rebuild` 双向扫描保证动力学可行。

- `KB-068`：AxisGroup/成员单写者仲裁与 MoveDirect 安全边界：组仍为 standby 时，linear/circular/MoveDirect/GroupHome 仅在所有成员 standstill 且无 base、sync、stream、superimposed 命令时接受，否则以 `invalid_argument` 在改写任何成员前原子拒绝；组活动后，成员公开的单轴 submit、override/retarget、superimposed、gear/cam/combine sync 入口同样以 `invalid_argument` 拒绝，owner 绑定与同步位置写入只经 `AxisGroup` 的私有 friend 入口。MoveDirect 没有 BufferMode，不加入 linear/circular 的 buffered/aborting 队列：全成员软限位与 OTG 预检通过后可立即接管当前协调运动；组级 Direct ID 仅用于组 FB 生命周期，成员 Direct/Halt 命令由各 AxisModel 分配本地 ID，避免与后续单轴完成账本串号；Direct 活动期间 linear/circular（含 aborting）和 Direct 重入报 `invalid_argument`，GroupSetOverride/GroupInterrupt 报 `unsupported`，拒绝均不扰动当前成员命令。自然全部到位才 Done；GroupStop 受控 Halt 后与 GroupDisable 立即取消后，原 MoveDirect FB 均报 CommandAborted；成员掉电或 ErrorStop 会取消其余成员、令组进入 ErrorStop，并使 MoveDirect FB 报 Error（`precondition_failed`）；上述终态在 Execute 下降沿前锁存。此批不是既有周期轨迹输出的声明变更，回放基线无需重录。语义规格见 `doc/compliance/part4-management-semantics.md`；验收进 `plcopen_core_r3_group_fb_tests` 与 `plcopen_core_part4_management_tests`。

- `KB-069`：L 系列批次 L0——IEC 61131-3 ST 逻辑子集 + 确定性字节码 VM（`core/st/`，纯增量新目录，规格 `doc/compliance/st-l0-semantics.md` 已批准 2026-07-11）。语言面：单 PROGRAM、六基本类型（BOOL/INT 16 位/DINT 32 位/REAL f32/LREAL f64/TIME int64 纳秒）、控制流全量（IF/CASE 值+子范围/FOR/WHILE/REPEAT/EXIT/RETURN）、`basic.h` 十个 IEC FB 命名形参直调（RTC 因日历壁钟显式排除，MC_* 归 L2）。**相对标准的声明口径**：无任何隐式类型转换（混型 = 编译错误，`*_TO_*` 归 L1）；整型溢出二补码环绕、除法向零截断、MOD 符号随被除数；除零/MOD 零编译期常量 = 编译错误、运行期 = scan fault；浮点走 IEEE（inf/NaN 传播不故障，NaN 比较恒假）；TIME 精度 = 任务周期（PT/ET 全程纳秒，到期判定天然 ceil；ET 读数可超 PT 一个周期内为 basic.h 既有合同）；逻辑运算全求值（无短路）；双字面量比较默认 DINT/LREAL；FOR 控制变量体内赋值 = 编译错误、TO 达类型极值的环绕由指令预算兜底；CTU/CTD/CTUD CV 回读环绕到 DINT；`reset()` 只清 fault 不复位变量（重新初始化 = 重载）。fault 语义：中止点前赋值保留、错误锁存至 `reset()`。看门狗 = 指令计数预算（确定性非墙钟）。字节码格式版本化，v1 不承诺跨版本兼容。不支持构造报专用 `unsupported_l1..l7/non_goal` 诊断码（归属批次机读）。验收：`plcopen_core_st_l0_{compiler,runtime,golden,quality}_tests` + `plcopen_core_st_fuzz`（10 万输入零 crash）+ `st-l0-conformance.yaml` 47 锚点 + 跨平台字节码锚点哈希门。非既有周期路径变更，回放基线不动。

- `KB-070`：L 系列批次 L1a——标量类型宇宙 + 转换矩阵机读化（`core/st/`，规格 `doc/compliance/st-l1a-semantics.md` 已批准 2026-07-11）。新增 10 标量：SINT/LINT、USINT/UINT/UDINT/ULINT（溢出模 2ⁿ 环绕）、BYTE/WORD/DWORD/LWORD（位模式类型：按位 AND/OR/XOR/NOT 与相等可用，**算术与排序比较 = 编译错误**）。**隐式转换唯一白名单**（有符号链/无符号链/窄无符号→宽有符号/REAL→LREAL/位串加宽，规范槽位形式下运行期零成本），其余组合显式；`<SRC>_TO_<DST>` 210 格全声明并机读（`st-l1a-conversions.yaml`，实现 dump 三方比对进 CTest）：整域变窄=截断回卷、浮→整=round-half-even（`TRUNC_*` 向零）且 NaN/Inf = 新 `conversion_invalid` scan fault、位串=位模式重解释、到 BOOL=非零为真（-0.0 为假）；TIME 仅 `TIME_TO_LINT`/`LINT_TO_TIME`（ns 直通）。运算面：`TIME */÷ 数值`（实数参与向零截断至 ns，整除零 fault、实除得非有限转 TIME = conversion_invalid）；`**` 幂仅 REAL/LREAL 底数（**运行值依平台 libm 允许差异、永不常量折叠**——折叠会把 pow 结果埋进常量池破坏跨平台字节码确定性）；CONTINUE；非正式 FB 调用（全输入按声明序，混用形式拒绝）；`VAR CONSTANT`（参与折叠、赋值拒绝）；类型化字面量 `TYPE#`。声明口径：TIME 缩放仅 TIME 在左；CASE/FOR 控制面维持 INT/DINT；一元负号仅有符号+实数；CTU 族 CV 回读环绕沿 KB-069。L0 字节码逐位不变（新指令仅追加，L0 锚点哈希 8149969011563308332 保持），L1a 扩展锚点哈希 4976297220763985794 入门禁。验收：`plcopen_core_st_l1a_{types,conversion}_tests` + matrix_check + fuzz 语料扩展。

- `KB-071`：L 系列批次 L2a-Bind——新增不透明 `AXIS_REF` 与宿主 `Instance::bind_axis`，首扫前允许覆盖绑定、运行后拒绝重绑；首批十个单轴 MC 块（Power/Home/Stop/Halt/MoveAbsolute/MoveRelative/MoveAdditive/MoveVelocity/SetOverride/Reset）的 ST PinTable 只由 `plcopen-motion-part1-io.yml` 生成，未绑定轴走 FB Error 而不令 scan fault。scan 属规划域；专项冻结窗口 1000 周期零分配，扩展 MC/AXIS_REF 语料 10 万 fuzz 通过，MoveAbsolute 与 C++ 门面逐周期 setpoint 逐位一致，SPSC committed-frame executor 冒烟通过。**边界**：现有轴层仅承载 BufferMode 0/1/2/5，3/4/6 明确 `invalid_argument`；Power 分方向使能、MoveAbsolute/Relative/Additive ContinuousUpdate、SetOverride AccFactor/JerkFactor/Busy 仅暴露官方引脚但没有 C++ 行为承载，合规声明继续标未通过；43/43 门面、B 级 22/43 审计时点与 D 项 4 关 16 开口径不因本批改变。规格与实现记录见 `doc/compliance/st-l2a-semantics.md`。

- `KB-072`：Part 5 P5-B 补齐此前缺失的六个 C++ 公开门面：StepBlock 只以独立实际 torque/velocity 连续保持判定堵转，DistanceCoded 只消费宿主定长唯一距离码表，HomeAbsolute 只消费宿主长寿命绝对位置槽，Flying Switch/Pulse 只在 standalone base motion 上被动捕获并整体平移活动剖面及排队绝对目标，AbortPassive 只撤销当前被动回零 owner。Flying 不改变活动命令 ID、速度、相对距离或物理剩余行程；平移越软限位/非有限目标进入 ErrorStop。**边界**：StepBlock 软件仿真不证明真机械堵转安全；不解析厂商绝对编码器协议、多圈状态或距离码格式；旧五块名称/I/O/语义偏差、标准派生类型和逐 I/O 声明仍开放，因此 11/11 有门面不等于 Part 5 合规。规格 `doc/compliance/part5-p5b-semantics.md`，验收 `plcopen_core_part5_homing_tests` 与 10 万周期零分配守卫；无既有回放声明变更。

---
