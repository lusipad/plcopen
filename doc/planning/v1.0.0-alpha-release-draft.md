# v1.0.0-alpha 发布草案（T3：AI 起草，需人工审签与发布）

状态：**已发布（2026-07-06，维护者指令执行）**。tag `v1.0.0-alpha` @
`dc23b09` 后的发布收口提交；D-LIC 维持现状 Apache 2.0（无文件变更）；
DoD 第 2/3 条按本草案建议作为 alpha 豁免项在 Release notes 如实标注；
v0.11.0 tag 已存在（检查单第 4 项历史完成）。本文件转为发布记录。

> **增量（2026-07-05 下午）**：Phase B 纯软件全部批次（KB-034~041）已并入
> alpha 范围——坐标系栈、kinematics v1（龙门/SCARA + 6R 预集成）、B9 轨迹流、
> cam C2 样条、look-ahead v2、adapters 软件面（ADR-0004/CiA402/bumpless）。
> 下文 R4 期草稿的能力清单以本增量与下方 07-06 增量为准。
>
> **增量（2026-07-06）**：机器人位姿闭环与软件收尾批全部并入——姿态管线
> （RPY + 6R 组接线，KB-042）、笛卡尔/位姿回读（RPY 反演万向节约定，
> KB-043）、段内笛卡尔插补（直线/圆弧/单后继 blending，逐周期逆解 + 测地
> 姿态，6R 实测 2.4µs/周期，KB-044/047/049）、腕奇异通过（KB-045）、cam
> 运动规律生成器（KB-046）、窗口深度可配（KB-048）、pyplcopen 位姿面
> （PoseArmSim + cam 工具函数）。发布 tag 快照为 36 项测试、17 份回放语料、
> KB-001~049；发布后的当前主线证据另见根 `STATUS.md`，不混入本发布快照。

## 发布定位（对外一段话）

`v1.0.0-alpha` 是 plcopen 重写内核（`core/`，L0-L7 分层架构）的首个消费入口检查点：
默认 CMake 包目标、install/export、FetchContent、demo 与 Python facade 全部指向新核，
v0.x 公开 FB 面与 pyplcopen 面已全量由新核承接。它**不是**商用级 PLC runtime，不含工业总线实证、实时性证据（72h 抖动报告）或
PLCopen 认证承诺，也不承诺 v1 ABI 稳定（alpha 语义）；kinematics/坐标系/轨迹流等
Phase B 纯软件能力已含（见上方增量）。

## Release Notes 草稿

### 亮点

- **新架构内核**：L0 rt（整型周期时间域、定长容器、SPSC、`-fno-exceptions` 兼容）→
  L1 otg（状态到状态 jerk-limited 规划，oracle + fuzz 门禁）→ L2-L4 几何/规划/执行 →
  L5 单写者轴/组模型 → L6 PLCopen FB 门面。依赖只向内，L0-L4 不含 PLCopen 语义。
- **v0.x FB 面全量承接**：单轴管理/运动、叠加/连续/附加、轨迹表、参数注册表、探针触发、
  数字 IO、DigitalCamSwitch、AxisInfo/MotionState、多轴同步（gear/cam/phasing/combine）、
  组管理与 Part 4 linear。名称与行为映射见[迁移指南](../migration-v0-to-v1.md)。
- **pyplcopen 面全量恢复**：`AxisSim` 与 v0.x 方法集对齐（含 stop/home_position/加速度读回）。
- **验证体系**：19 项 CTest（含 6 个族级验收套件）、OTG oracle + fuzz、RT-safety 静态扫描、
  黄金回放回归（逐周期 setpoint 比对，未声明变更零差异）、微基准趋势、B9-lite 轨迹流 demo。
- **时间最优运动规划（A9 v1）**：离散运动经近时间最优 7 段 jerk-limited S 曲线规划，
  同等约束下运动时长约为保守基线的 65%（KB-026，回放基线 v2）。
- **已声明行为边界编号化**：新核相对 v0.x 的 8 条有意偏离固定为 `KB-019`..`KB-026`
  （README 已知边界节）。

### 破坏性变更

- 默认 `plcopen::plcopen` 为 header-only 新核目标；旧 `src/` 线需显式
  `-DPLCOPEN_BUILD_LEGACY=ON`（安装另需 `PLCOPEN_INSTALL_LEGACY=ON`）。
- 命名空间 `plcopen::core::*`、snake_case 字段、`rt::ErrorCode` 错误码；调度器被显式
  `cycle()` 取代。完整对照表见[迁移指南](../migration-v0-to-v1.md)。

### 已知限制（alpha 边界）

- 无工业总线实证（EtherCAT 属独立仓库 `plcopen-fieldbus`，硬件阶段）；
  kinematics/坐标系/位姿/笛卡尔插补已含（见 07-06 增量），其声明边界见
  KB-036~049。
- 无实时性声明：72h PREEMPT_RT 抖动报告未产出（模板已备，需真机）。
- OTG 求解器为近时间最优 7 段 jerk-limited（A9 v1，随机状态域总时长约为保守基线的 65%，
  百万级 fuzz 门禁）；非零边界加速度的完整 case 枚举是后续项（当前显式报 `unsupported`）。
- 真实硬件 Servo 虚接口未定义（L7 适配层设计项，Phase B5/B7）；当前由 `AxisModel`
  适配器钩子暂代。
- 不承诺 ABI 稳定；PLCopen 认证不在本版本范围。

## rewrite-plan §5 DoD 对照（发布阻塞项清单）

| # | DoD 条目 | 状态 | 证据/缺口 |
|---|----------|------|-----------|
| 1 | 全量测试绿 + 覆盖率 ≥ 旧核基线（>50%，目标 70%+） | ✅ | 19/19 CTest；全套件合并行覆盖 85.37%（Debug 口径，见证据包 2026-07-05 补记） |
| 2 | 回放语料 diff 审签 | ✅ 已关闭（2026-07-06） | 新核黄金回放 18 语料零差异护栏 + **老-新语义等价 harness**（legacy_compare：3 项终点合同逐位相等、停车合同完成，SEMANTIC_EQUIVALENCE 全 PASS）；逐周期一致因 KB-026~050 声明变更本就不可能，合同级等价即为承接证据 |
| 3 | 基准：新核周期耗时 ≤ 旧核 50%；freeze 后分配断言 72h 零触发 | ✅（周期等效口径，07-11 回写） | 老-新对照实测 core = 旧线 **9.3%**（≤50% PASS，legacy_compare 复跑）。72h soak：07-06 13:02 启动的分离进程**证据链断裂**（无结束日志，如实登记）；2026-07-11 22:56 +08 交付周期等效证据 `plcopen_core_a2_alloc_guard --cycles 2592000000`（72h@1kHz 周期数 ×10）Release 零分配 PASS——分配断言敏感维度是周期数而非墙钟；真实 72h 墙钟连续运行（含抖动）归 B7 真机 RT 报告。口径调整开放维护者复核 |
| 4 | find_package / FetchContent / pyplcopen smoke | ✅ | consumer + smoke 全部 CTest 绿 |
| 5 | 迁移指南 + PROVENANCE + LICENSE 一致 | ⚠️ 待审签 | 迁移指南完整；PROVENANCE.md 仍为 T3 草案，D-LIC 决策（rewrite-plan 2.6）未拍板 |
| 6 | v0.x EOL 公告发布 | ⚠️ 人工 | 草案见 [r4-evidence-package.md](../archive/r4-evidence-package.md)，随 Release 一起发布 |

## 发布前人工检查单

1. 审阅两轮迁移提交（重点：T2 变更与 `KB-019`..`KB-025` 边界）。
2. 拍板 D-LIC（保持 Apache 2.0 / MIT / 双许可），使 PROVENANCE/LICENSE/NOTICE 口径一致。
3. 裁定 DoD 第 2/3 条是否作为 alpha 豁免项（在 Release notes「已知限制」中如实标注）。
4. 先补发 `v0.11.0` tag/Release（旧线最后功能检查点，回放基线锚点）。
5. 打 `v1.0.0-alpha` tag，粘贴 Release notes 草稿，附 CI 链接与门禁清单。
6. 发布 EOL 公告（90 天 P0-only 窗口自发布日起算）。

---

*本草案随证据包维护；发布后归档为 release 记录。*
