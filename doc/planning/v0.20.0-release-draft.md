# v0.20.0 发布草案（tag 与 GitHub Release 需维护者明确授权）

状态：**发布候选已验证，待授权发布（2026-07-20）**。版本面与发布资料已
对齐到 `0.20.0`，本地门禁与七组远端发布门禁全部通过；PyPI trusted
publisher 已由维护者注册，GitHub `pypi` environment 已验证存在。尚未创建
tag、PyPI 包或 GitHub Release；PyPI JSON API 在首次发布前返回 404 属预期。

`v1.0.0-alpha` 保留为 2026-07-06 的历史实验性预览，不删除、不改写。
本次回到 0.x 是成熟度校准：项目尚未形成稳定 ABI、真实硬件长期证据、
PLCopen 认证、灯塔用户与维护周期承诺，不能用 1.0 版本号暗示这些条件已经
成立。

## 发布形式

### 1. GitHub Release（主发布页）

- 普通 0.x 版本：`v0.20.0`，发布后标记为 Latest，不标记 prerelease；
  “非 prerelease”不等于 1.0 稳定性承诺。
- C++ 交付：GitHub 自动生成的 source `.zip` / `.tar.gz`，其中包含
  C++17 header-only 新核、CMake install/export、Conan recipe 与消费示例。
- 不附加 Windows/Linux/macOS 的 C++ 二进制安装器。header-only 库没有
  必要制造平台二进制矩阵，源码包才是可复现、可审计的交付物。
- vcpkg 清单仍是草稿，Conan recipe 仍需用户从源码创建；本次不宣称已进入
  vcpkg/ConanCenter 公共仓库。

### 2. PyPI（Python 消费入口）

- 包：`pyplcopen==0.20.0`。
- 资产：CPython 3.10～3.13 的 Windows、manylinux x86_64、macOS x86_64/
  arm64 wheels，以及一个 sdist。
- tag 推送触发 `.github/workflows/wheels.yml`，通过 Trusted Publishing/OIDC
  发布，不保存长期 PyPI token。
- 前置条件：维护者先在 PyPI 注册 `pyplcopen` trusted publisher，并在
  GitHub 仓库创建 `pypi` environment。两项未完成时不得推 tag，否则构建
  可以成功但发布作业会失败。

### 3. 文档站

- 文档继续发布到 <http://lusipad.com/plcopen/>。
- Release notes 链接 STATUS、CHANGELOG、迁移指南、已知边界和商用证据总账；
  不复制另一套容易漂移的能力说明。

## GitHub Release 文案

面向使用者的正式发布记录已拆分到
[`docs/releases/v0.20.0.md`](../../docs/releases/v0.20.0.md)。该文件使用纯
GitHub Markdown，可直接作为 GitHub Release body，同时进入公开文档站。
本页只保留发布形式、门禁证据与操作检查单，不再复制另一份发布叙事。

## 发布门禁

以下证据来自候选代码与发布元数据提交 `5a5cf81`；本轮后续只同步文档，
不改变源码、构建配置或包内容。tag 前仍需确认最终 PR 头部的主线 CI 为绿：

| 门禁 | 要求 | 状态 |
|------|------|------|
| 本地 Debug build + CTest | 全量已登记测试通过 | ✅ 91/91（含 11 fuzz） |
| RT scan + replay fixtures | 通过，黄金语料无未声明变化 | ✅ 27 文件；18 语料 / 2409 samples |
| Windows CI | build/test/coverage/consumer 通过 | ✅ [run 29692190516](https://github.com/lusipad/plcopen/actions/runs/29692190516)，0 annotations |
| Linux CI | GCC/Clang/ARM64、clang-tidy、docs/consumer 通过 | ✅ [run 29692190521](https://github.com/lusipad/plcopen/actions/runs/29692190521)，3 job 均 0 annotations |
| Wheels | Windows/Linux/macOS wheels + sdist 构建通过 | ✅ [run 29692204896](https://github.com/lusipad/plcopen/actions/runs/29692204896)，publish 因非 tag 正确跳过 |
| Core Nightly | fuzz、sanitizer/TSAN 等全部通过 | ✅ [run 29692206177](https://github.com/lusipad/plcopen/actions/runs/29692206177)，7/7 job |
| Coverage Gate | line/branch 门槛通过 | ✅ [run 29692207269](https://github.com/lusipad/plcopen/actions/runs/29692207269)：line 95.5%，运动栈 branch 85.0%，ST branch 85.1% |
| Mutation Score Gate | 20 项登记 mutation，总 kill score >= 70% | ✅ [run 29692208327](https://github.com/lusipad/plcopen/actions/runs/29692208327)：20/20，100% |
| Documentation | MkDocs 严格构建与部署通过 | ✅ 本地 strict + [run 29692209416](https://github.com/lusipad/plcopen/actions/runs/29692209416) |
| GitHub Actions runtime | 仓库可控工作流无 Node 20 弃用警告 | ✅ Windows/Linux/Wheels/Docs 运行时注解为 0 |

## 发布检查单

1. 审阅本草案、CHANGELOG 和版本成熟度边界。
2. 在 PyPI 创建/确认 `pyplcopen` 项目 Trusted Publisher：owner `lusipad`、
   repository `plcopen`、workflow `wheels.yml`、environment `pypi`。（已完成）
3. 在 GitHub 仓库创建 `pypi` environment；无需配置 API token secret。（已完成）
4. 确认发布候选 commit 的全部门禁为绿。
5. 维护者明确授权版本与目标提交后，由维护者或 AI 创建并推送 annotated
   tag `v0.20.0`。
6. 等待 tag 触发的 Wheels 与 PyPI publish 成功，下载并验证 wheel/sdist。
7. 经维护者明确授权后创建 GitHub Release，使用
   `docs/releases/v0.20.0.md` 作为 body，检查 tag 固定链接并标记 Latest。
8. 发布后把本页、STATUS 与 ROADMAP 的状态从“候选”更新为“已发布”。

PyPI publisher 注册仍是维护者账号动作。发布 tag 与 GitHub Release 未获明确
授权时，AI 只准备提交、验证证据与可粘贴文案；获得版本与目标提交授权后可
代为执行。
