# v0.21.0 发布执行记录

状态：**已发布（2026-07-30）**。本轮先以候选提交
`b4f2b87ea7a2cf5bdd0a9ee262dc5ddbcf1ca807` 完成 9/9 候选 CI，全套发布门禁
闭合后由维护者授权创建并推送 annotated tag `v0.21.0`（object
`9985312fd8c862ae435bec28adcd5f2943966288` → target
`08d62b1b16e841a88509284b4c8c75d778592efc`）。
[GitHub Release](https://github.com/lusipad/plcopen/releases/tag/v0.21.0) 为
Latest，[PyPI `pyplcopen==0.21.0`](https://pypi.org/project/pyplcopen/0.21.0/)
已公开且未撤回，含 20 个 wheels 与 1 个 sdist。

## 版本判断

本轮采用 `v0.21.0`，不采用 `v0.20.1`：

- `v0.20.0..main` 包含 H1/H2/H3、T2a/T2b、D1/D3、Z1～Z5、X5、
  Y4b/Y7b1/Y7b2a 等新增能力，不是只修复既有版本缺陷的补丁批；
- 未发现把当前版本推进到 1.0 的新证据。稳定 ABI、真实硬件长期验证、
  PLCopen/Safety 认证、灯塔用户与维护周期承诺仍未形成；
- `v1.0.0-alpha` 继续保留为历史实验性预览，不删除、不改写。

## 发布形式

### GitHub Release

- 普通 0.x 版本：`v0.21.0`，发布后标记为 Latest，不标记 prerelease；
- C++ 继续以 GitHub 自动生成的 source `.zip` / `.tar.gz` 交付，其中包含
  C++17 header-only 内核、CMake install/export、本地 Conan recipe、示例与
  文档；
- 不新增独立 C++ 平台安装器或共享库二进制。

### PyPI

- 包：`pyplcopen==0.21.0`；
- `v*` tag 触发 `.github/workflows/wheels.yml`，构建 CPython 3.10～3.13 的
  Windows、manylinux x86_64、macOS x86_64/arm64 wheels 与一个 sdist；
- 只有 tag push 才执行 Trusted Publishing/OIDC 发布。候选阶段只手动构建
  wheels/sdist，`publish_pypi` 必须按条件跳过；
- 现有 `pypi` environment 与 Trusted Publisher 沿用 `v0.20.0` 已验证配置。

### C++ 包管理器边界

- 根 `.version`、CMake、Conan recipe 与 vcpkg manifest 跟随源码更新到
  `0.21.0`；
- `packaging/conan-center/` 与 `ports/plcopen/` 是中央 registry 提交资产，
  当前仍固定已公开且有真实归档哈希的 `v0.20.0`；`v0.21.0` 的中央 registry
  更新仍需独立提交和上游维护者流程，不冒充已随本仓发布同步完成。

### 文档与工具

- 面向用户的 GitHub Release body 使用
  [`docs/releases/v0.21.0.md`](../../docs/releases/v0.21.0.md)，中文镜像为
  [`docs.zh/releases/v0.21.0.md`](../../docs.zh/releases/v0.21.0.md)；
- D1 ST Language Server 随 Python 源码/包进入本版本；VS Code 扩展仍只形成
  可复验的本地 VSIX/CI artifact，不声明已发布到 Marketplace；
- 文档继续发布到 <https://lusipad.com/plcopen/> 与 `/en/` 英文镜像。

## 发布门禁

以下证据已形成最终发布闭环；候选阶段与 tag 阶段证据分开记录：

| 门禁 | 要求 | 当前状态 |
|------|------|----------|
| 版本一致性与 sdist | `.version` / CMake / Python / Conan / 根 vcpkg 均为 0.21.0；sdist 名称正确 | 本地通过：5/5 版本面一致；`pyplcopen-0.21.0.tar.gz` |
| 本地 Debug build + CTest | 全量已登记测试通过 | 本地通过：MSVC Debug，102/102 |
| RT scan + replay fixtures | 固定范围通过，黄金语料无未声明变化 | 本地通过：32 个文件；18 个 fixture / 2409 个样本 |
| Windows CI | build/test/coverage/consumer 通过 | 候选提交 `b4f2b87ea7a2cf5bdd0a9ee262dc5ddbcf1ca807` 已通过（计入 9/9 候选 CI 全绿） |
| Linux CI | GCC/Clang/ARM64、clang-tidy、consumer 通过 | 候选提交 `b4f2b87ea7a2cf5bdd0a9ee262dc5ddbcf1ca807` 已通过（计入 9/9 候选 CI 全绿） |
| Language Tools | Windows/Linux wheel、LSP、VSIX 与许可证门通过 | 候选提交 `b4f2b87ea7a2cf5bdd0a9ee262dc5ddbcf1ca807` 已通过（计入 9/9 候选 CI 全绿） |
| Twin integration | Windows/Linux T2a/T2b/Scope 集成通过 | 候选提交 `b4f2b87ea7a2cf5bdd0a9ee262dc5ddbcf1ca807` 已通过（计入 9/9 候选 CI 全绿） |
| Wheels | Windows/Linux/macOS wheels + sdist 构建通过，publish 正确跳过 | 候选提交 `b4f2b87ea7a2cf5bdd0a9ee262dc5ddbcf1ca807` 已通过（计入 9/9 候选 CI 全绿） |
| Core Nightly | fuzz、soak、ASan/UBSan、TSan 与两平台进程门全部通过 | 候选提交 `b4f2b87ea7a2cf5bdd0a9ee262dc5ddbcf1ca807` 已通过（计入 9/9 候选 CI 全绿） |
| Coverage Gate | full-core line >= 90%，运动栈 branch >= 85%，ST branch >= 85% | 候选提交 `b4f2b87ea7a2cf5bdd0a9ee262dc5ddbcf1ca807` 已通过（计入 9/9 候选 CI 全绿） |
| Mutation Score Gate | 当前登记 mutation kill score >= 70% | 候选提交 `b4f2b87ea7a2cf5bdd0a9ee262dc5ddbcf1ca807` 已通过（计入 9/9 候选 CI 全绿） |
| Documentation | 中英路径、语言合同、MkDocs strict、Doxygen 与 Pages 通过 | 候选提交 `b4f2b87ea7a2cf5bdd0a9ee262dc5ddbcf1ca807` 已通过（计入 9/9 候选 CI 全绿） |
| 最终 `main` 门禁 | tag 目标提交的发布相关主线门全部通过 | 目标 `08d62b1b16e841a88509284b4c8c75d778592efc` 的 Windows `30480823667`、Linux `30480823343`、Language Tools `30480823421`、Twin `30480823345` 与 Documentation `30480824589` 全绿 |
| tag 发布 | wheels、sdist、PyPI publish 全绿并完成公开下载校验 | [tag workflow](https://github.com/lusipad/plcopen/actions/runs/30496916708) 5/5 job 全绿；PyPI 已公开 20 个 wheels 与 1 个 sdist |
| Cold User | 公开入口以发布版本重新安装/消费并通过 docs entrypoints | [run 30497974380](https://github.com/lusipad/plcopen/actions/runs/30497974380) 已用 `python-version=0.21.0`、`source-ref=v0.21.0`、`docs-base-url=https://lusipad.com/plcopen/en` 复验 Notebook、C++ installed package、ST 与 docs entrypoints，全部通过 |

两项曾被特别关注的旧证据，均已在最终发布前重新闭合：

- 旧提交 `8b932d6` 的 Coverage Gate 曾因运动栈 84.5%、ST 84.3% 失败；
  PR #37 的公开行为边界补测已恢复固定门，并在候选提交
  `b4f2b87ea7a2cf5bdd0a9ee262dc5ddbcf1ca807` 的 9/9 候选 CI 中重新闭合；
- 同一旧提交最近一次 Core Nightly 的 executor TSan lane 曾出现参考 executor
  `EXECUTOR FAIL`；最终发布前已在候选提交
  `b4f2b87ea7a2cf5bdd0a9ee262dc5ddbcf1ca807` 的 9/9 候选 CI 中重新通过。

## 发布执行顺序

1. 对齐版本元数据、CHANGELOG、双语 Release body、公开入口与本记录。
2. 运行本地版本一致性、sdist、Debug build、全量 CTest、RT scan、replay、
   双语 MkDocs strict 与链接检查。
3. 推送候选提交 `b4f2b87ea7a2cf5bdd0a9ee262dc5ddbcf1ca807`，闭合
   Windows、Linux、Language Tools、Twin、Documentation 五条主门。
4. 在同一候选提交手动运行 Wheels、Core Nightly、Coverage Gate 与
   Mutation Score Gate，并形成 9/9 候选 CI 全绿。
5. 发布前确认最终目标提交与主线门禁口径。
6. 维护者明确指定“发布 `v0.21.0`，目标
   `08d62b1b16e841a88509284b4c8c75d778592efc`”后，创建并推送 annotated
   tag `v0.21.0`（object `9985312fd8c862ae435bec28adcd5f2943966288`）。
7. 等待 tag Wheels/PyPI 作业完成，并校验包版本与公开下载。
8. 使用 `docs/releases/v0.21.0.md` 创建 GitHub Release，标记 Latest、
   非 prerelease。
9. 把 README、STATUS、ROADMAP、本页和文档站更新为“已发布”；中央 registry
   继续保持 `v0.20.0`，等待独立提交与上游维护者流程。

## 收尾复跑记录

- 首次根 docs URL 的公开 Cold User run
  [30497861595](https://github.com/lusipad/plcopen/actions/runs/30497861595)
  因 notebook 入口 404 失败，根因是 `cold-user.yml` 默认英文 docs root 仍指向
  `/plcopen` 而非 `/plcopen/en`。
- 主线已将 `.github/workflows/cold-user.yml` 的默认英文 `docs-base-url` 修正为
  `https://lusipad.com/plcopen/en`，随后公开 Cold User run
  [30497974380](https://github.com/lusipad/plcopen/actions/runs/30497974380)
  以 `python-version=0.21.0`、`source-ref=v0.21.0` 复跑通过，Notebook、C++
  installed package、ST 与 docs entrypoints 全绿。

本轮发布中，tag / GitHub Release 仍由维护者明确指定版本与目标提交后执行；
LICENSE/NOTICE/PROVENANCE、账号、签署、法务与外部承诺仍只由人落地。
